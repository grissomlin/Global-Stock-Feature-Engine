name: Global Stock Matrix Monitor

on:
  workflow_dispatch:
    inputs:
      market:
        description: '選擇執行市場 (all / tw / us / cn / hk / jp / kr)'
        required: true
        default: 'all'
        type: choice
        options: [all, tw, us, cn, hk, jp, kr]
  schedule:
    - cron: '0 6 * * 1-5'

jobs:
  prepare-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix_list: ${{ steps.set-matrix.outputs.matrix_list }}
    steps:
      - id: set-matrix
        run: |
          if [[ "${{ github.event.inputs.market }}" == "all" || "${{ github.event_name }}" == "schedule" ]]; then
            echo "matrix_list=[\"tw\", \"us\", \"cn\", \"hk\", \"jp\", \"kr\"]" >> $GITHUB_OUTPUT
          else
            echo "matrix_list=[\"${{ github.event.inputs.market }}\"]" >> $GITHUB_OUTPUT
          fi

  execute-task:
    needs: prepare-matrix
    runs-on: ubuntu-latest
    timeout-minutes: 180 
    strategy:
      fail-fast: false
      matrix:
        market: ${{ fromJson(needs.prepare-matrix.outputs.matrix_list) }}
    
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      GDRIVE_SERVICE_ACCOUNT: ${{ secrets.GDRIVE_SERVICE_ACCOUNT }}
      GH_PAT: ${{ secrets.GH_PAT }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip' # 💡 快取 Python 套件

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install resend google-api-python-client google-auth-httplib2 google-auth-oauthlib python-dotenv pandas-ta tqdm yfinance

      # 🚀 新增：資料庫檔案快取 (GitHub Cache)
      - name: 💾 Restore Market Database Cache
        id: db-cache
        uses: actions/cache@v3
        with:
          path: |
            *.db
          # 使用市場代碼作為 Key，確保六台機器不會互相覆蓋
          # 這裡使用 run_id 的前綴來確保每次執行都有機會儲存新的內容
          key: ${{ matrix.market }}-db-v1-${{ github.run_id }}
          restore-keys: |
            ${{ matrix.market }}-db-v1-

      - name: Run Market Update
        run: |
          # main.py 會先檢查本地是否有快取恢復的 .db，如果沒有才去 Google Drive 抓
          python -u main.py ${{ matrix.market }}

      # 📢 觸發連動 (維持原樣)
      - name: 📢 Trigger Alpha Data Lab Sync
        if: success()
        run: |
          DB_NAME="${{ matrix.market }}_stock_warehouse"
          curl -X POST https://api.github.com/repos/grissomlin/Alpha-Data-Cleaning-Lab/dispatches \
          -H "Accept: application/vnd.github.v3+json" \
          -H "Authorization: bearer $GH_PAT" \
          -d "{\"event_type\": \"warehouse_sync_complete\", \"client_payload\": {\"market_db\": \"$DB_NAME\"}}"

      - name: Notify System Failure
        if: failure()
        run: |
          python -c "
          import os, requests
          token = os.getenv('TELEGRAM_BOT_TOKEN')
          chat_id = os.getenv('TELEGRAM_CHAT_ID')
          market = '${{ matrix.market }}'.upper()
          msg = f'❌ 【系統崩潰】市場: {market}\n原因：GitHub Actions 執行失敗。'
          if token and chat_id:
              requests.post(f'https://api.telegram.org/bot{token}/sendMessage', json={'chat_id': chat_id, 'text': msg})
          "
